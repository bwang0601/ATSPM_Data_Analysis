---
title: "07_data"
author: "Bruce Wang"
date: "11/13/2019"
output: html_document
editor_options: 
  chunk_output_type: console
---

```{r}
library(tidyverse)
```

```{r}
dfAMPeak_800N <- read_rds("data/dfAMPeak_800N.rds")
dfMidDay_800N <- read_rds("data/dfMidDay_800N.rds")
```

## K-means Cluster Analysis
```{r}
# Setup Cluster variables
set.seed(3)

cluster_variables_800N <- c("SplitFailures", "ArrivalsOnGreen", "PlatoonRatio", "TotalRedLightViolations", "PercentForceOffs")

# Create Table without NA Volue
complete_dfAMPeak_800N <- na.omit(dfAMPeak_800N)

complete_dfMidDay_800N <- na.omit(dfMidDay_800N)

# AMPeak Normal and Scaled clusters
clusters_AMPeak_800N <- complete_dfAMPeak_800N %>%
  select(cluster_variables_800N) %>%
  kmeans(centers = 5)

scaled_clusters_AMPeak_800N <- complete_dfAMPeak_800N %>%
  select(cluster_variables_800N) %>%
  mutate_all(scale) %>% # want to scale the data to make it normalized for kmeans
  kmeans(centers = clusters_AMPeak_800N$centers %>% scale() )

clustered_dfAMPeak_800N <- complete_dfAMPeak_800N %>%
  mutate(cluster = clusters_AMPeak_800N$cluster,
         scaled_cluster = scaled_clusters_AMPeak_800N$cluster) %>% 
  write_rds("data/clustered_dfAMPeak_800N.rds")

# MidDay Normal and Scaled clusters
clusters_MidDay_800N <- complete_dfMidDay_800N %>%
  select(cluster_variables_800N) %>%
  kmeans(centers = 5)

scaled_clusters_MidDay_800N <- complete_dfMidDay_800N %>%
  select(cluster_variables_800N) %>%
  mutate_all(scale) %>% # want to scale the data to make it normalized for kmeans
  kmeans(centers = clusters_MidDay_800N$centers %>% scale() )

clustered_dfMidDay_800N <- complete_dfMidDay_800N %>%
  mutate(cluster = clusters_MidDay_800N$cluster,
         scaled_cluster = scaled_clusters_MidDay_800N$cluster) %>% 
  write_rds("data/clustered_dfMidDay_800N.rds")
```
How different are the clusterings? Turns out they are very different!

```{r}
clustered_dfAMPeak_800N %>%
  group_by(cluster, scaled_clusters_AMPeak_800N) %>%
  tally() %>%
  spread(scaled_clusters_AMPeak_800N, n, fill = 0) %>%
  knitr::kable()
```

Going forward, we will make only plots using the scaled clutering algorithm.

## Distributions {.tabset}

We want to try to categorize the clusters based on their values for specific performance measures to see
if the clustering can tell us, for instance, if an intersection is performing well or poorly based on
its cluster assignment alone.

### SplitFailures AMPeak
```{r}
clustered_dfAMPeak_800N %>%
  ggplot(aes(x = SplitFailures)) + geom_bar() + facet_wrap(~scaled_clusters_AMPeak_800N)
```

### SplitFailures MidDay
```{r}
clustered_dfMidDay_800N %>%
  ggplot(aes(x = SplitFailures)) + geom_bar() + facet_wrap(~scaled_clusters_MidDay_800N)
```
Now let's make a table where the percent of cycles ending in split failure by cluster can be plotted.

```{r}
clustered_dfAMPeak_800N %>%
  group_by(scaled_clusters_AMPeak_800N) %>%
  summarize(
    `0` = sum(ifelse(SplitFailures == 0, TRUE, FALSE)) / n(),
    `0- 3` = sum(ifelse(SplitFailures <= 3, TRUE, FALSE)) / n(),
    `> 3` = sum(ifelse(SplitFailures > 3, TRUE, FALSE)) / n()
  )

clustered_dfMidDay_800N %>%
  group_by(scaled_clusters_MidDay_800N) %>%
  summarize(
    `0` = sum(ifelse(SplitFailures == 0, TRUE, FALSE)) / n(),
    `0- 3` = sum(ifelse(SplitFailures <= 3, TRUE, FALSE)) / n(),
    `> 3` = sum(ifelse(SplitFailures > 3, TRUE, FALSE)) / n()
  )

clustered_dfAMPeak_800N %>%
  group_by(scaled_clusters_AMPeak_800N) %>%
  summarize(
    `0` = sum(ifelse(ArrivalsOnRed == 0, TRUE, FALSE)) / n(),
    `1- 3` = sum(ifelse(ArrivalsOnRed< 3, TRUE, FALSE)) / n(),
    `> 4` = sum(ifelse(ArrivalsOnRed > 4, TRUE, FALSE)) / n()
  )
```

## Plots {.tabset}

I wonder if we can code up some kind of discrimination function based on the clusters. Like, 
draw the blue line so it contains 80% of points in cluster 1.

```{r}
# blue_level <- clustered_df %>%
#   filter(scaled_cluster == 1) %>%
#   select(scaled_cluster, ForceOffs) %>%
#   group_by(ForceOffs) %>% tally() %>%
#   mutate(p = n / sum(n),
#          cump = cumsum(p)) %>%
#   filter(cump < 0.85) %>%
#   slice(nrow(.)) %>% .$ForceOffs
# 
# ggplot(clustered_df, aes(x = TotalVolume, y = ForceOffs, color = factor(scaled_cluster))) +
#   geom_abline(slope = 30/200, intercept = 0, col = "red")+
#   geom_abline(slope = 30/400, intercept = -5, col = "red")+
#   geom_abline(slope = 30/600, intercept = -10, col = "red")+
#   geom_abline(slope = 30/800, intercept = -15, col = "red")+
#   geom_hline(yintercept = blue_level, col = "blue")+
#   geom_point(alpha = 0.3) 
# ggplot(clustered_df, aes(x = TotalVolume/TotalCycles, y = ForceOffs/TotalCycles, color = factor(scaled_cluster))) +
#   geom_point(alpha = 0.3)
```

### PlatoonRatio VS PercentAOG AMPeak
```{r}
ggplot(clustered_dfAMPeak_800N, aes(x = PlatoonRatio, y = PercentAOG, color = factor(scaled_clusters_AMPeak_800N))) +
  geom_point(alpha = 0.3)+
  xlim(0,2.5) + ylim(0.00, 1.00) +
  theme_bw()

```

### PlatoonRatio VS PercentAOG MidDay
```{r}
ggplot(clustered_dfMidDay_800N, aes(x = PlatoonRatio, y = PercentAOG, color = factor(scaled_clusters_MidDay_800N))) +
  geom_point(alpha = 0.3) +
  xlim(0,2.5) + ylim(0.00, 1.00) +
  theme_bw()

```

### TotalVolume VS PercentAOG AMPeak
```{r}
ggplot(clustered_dfAMPeak_800N, aes(x = TotalVolume, y =PercentAOG, color = factor(scaled_clusters_AMPeak_800N))) +
  geom_point(alpha = 0.3) +
  xlim(0,850) + ylim(0.00, 1.00) +
  theme_bw()
```

### TotalVolume VS PercentAOG MidDay
```{r}
ggplot(clustered_dfMidDay_800N, aes(x = TotalVolume, y =PercentAOG, color = factor(scaled_clusters_MidDay_800N))) +
  geom_point(alpha = 0.3) + 
  xlim(0,850) + ylim(0.00, 1.00) +
  theme_bw()
```

### TotalVolume VS SplitFailures AMPeak
```{r vol-vs-red-AM}
ggplot(clustered_dfAMPeak_800N, aes(x = TotalVolume/TotalCycles, y = SplitFailures/TotalCycles, color = factor(scaled_clusters_AMPeak_800N))) +
  geom_point(alpha = 0.3) +
  xlim(0,125) + ylim(0.00, 1.00) +
  theme_bw()
```

### TotalVolume VS SplitFailures MidDay
```{r vol-vs-red-MD}
ggplot(clustered_dfMidDay_800N, aes(x = TotalVolume/TotalCycles, y = SplitFailures/TotalCycles, color = factor(scaled_clusters_MidDay_800N))) + 
  geom_point(alpha = 0.3) +
  xlim(0,125) + ylim(0.00, 1.00) +
  theme_bw()
```

### PlatoonRatio VS Split Failures AMPeak
```{r}
ggplot(clustered_dfAMPeak_800N, aes(x = PlatoonRatio, y = SplitFailures/TotalCycles, color = factor(scaled_clusters_AMPeak_800N))) +
  geom_point(alpha = 0.3) +
  xlim(0,2.5) + ylim(0.00, 1.00) +
  theme_bw()
```

### PlatoonRatio VS Split Failures AMPeak
```{r}
ggplot(clustered_dfMidDay_800N, aes(x = PlatoonRatio, y = SplitFailures/TotalCycles, color = factor(scaled_clusters_MidDay_800N))) +
  geom_point(alpha = 0.3) + 
  xlim(0,2.5) + ylim(0.00, 1.00) +
  theme_bw()
```

### TotalVolume VS TotalRedLightViolations AMPeak
```{r vol-vs-redlight-AM}
ggplot(clustered_dfAMPeak_800N, aes(x = TotalVolume/TotalCycles, y = TotalRedLightViolations/TotalCycles, color = factor(scaled_clusters_AMPeak_800N))) +
  geom_point(alpha = 0.3) +
  xlim(0, 125) + ylim(0, 5) +
  theme_bw()
```

### TotalVolume VS TotalRedLightViolations MidDay
```{r vol-vs-redlight-MD}
ggplot(clustered_dfMidDay_800N, aes(x = TotalVolume/TotalCycles, y = TotalRedLightViolations/TotalCycles, color = factor(scaled_clusters_MidDay_800N))) +
  geom_point(alpha = 0.3) +
  xlim(0, 125) + ylim(0, 5) +
  theme_bw()
```

### ForceOffs VS Total Volume AMPeak
```{r ForceOffs VS Total Volume-AM}
ggplot(clustered_dfAMPeak_800N, aes(x = TotalVolume/TotalCycles, y = PercentForceOffs, color = factor(scaled_clusters_AMPeak_800N))) +
  geom_point(alpha = 0.3) +
  xlim(0, 125) + ylim(0, 1.5) +
  theme_bw()
```

### ForceOffs VS Total Volume MidDay
```{r ForceOffs VS Total Volume-DM}
ggplot(clustered_dfMidDay_800N, aes(x = TotalVolume/TotalCycles, y = PercentForceOffs, color = factor(scaled_clusters_MidDay_800N))) +
  geom_point(alpha = 0.3) +
  xlim(0, 125) + ylim(0, 1.5) +
  theme_bw()
```
